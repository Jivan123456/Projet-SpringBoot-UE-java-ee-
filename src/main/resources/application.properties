
spring.datasource.url=jdbc:mysql://localhost:3306/Database1?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=jivan
spring.datasource.password=1234


# create-drop recrée les tables à chaque démarrage (pour dev/test)
# Utilisez 'update' si on veut  conserver les données entre les redémarrages
spring.jpa.hibernate.ddl-auto=create-drop

# Afficher les requêtes SQL dans la console ( pour déboguer)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false


server.port=8889


